# 项目状态

当前目标：制作一个无前端、服务端/客户端分离、面向 AI 模型交互的洛克王国世界对战模拟器。

当前核心形态：6v6 固定对战，服务端 + 两个客户端，三个终端运行。

## 运行方式

```bash
# 终端 1
python server.py --port 5000

# 终端 2
python client.py --port 5000

# 终端 3
python client.py --port 5000
```

客户端支持 `--auto` 自动战斗模式。

## 目录结构

```text
server.py                 # 对战服务端
client.py                 # 命令行客户端
data/
  spirits.json            # 精灵数据
  skills.json             # 技能数据
  typechart.json          # 属性克制
  natures.json            # 性格
  buff.json               # 增益/减益规则（仅 desc 用自然语言，其他为代码字段）
  test_team.json          # A 默认队伍
  test_team_b.json        # B 默认队伍
sim/
  battle.py               # 战斗主循环、状态序列化
  models.py               # BattlePet / BattleSkill / BattleState / Action
  damage.py               # 伤害计算
  data_loader.py          # 数据加载、面板计算
  buffs.py                # 增益/减益、纯减益、回合结束结算
  marks.py                # 印记系统：正/负面各一槽，新印记顶掉同类旧印记
  burst.py                # 迸发效果：入场后首次行动消耗
  evolution.py            # 进化链、萌化退化
  typechart.py            # 属性克制查询
logs/
  battle/
    server.log
    clientA.log
    clientB.log
```

## 当前已实现机制

- 6v6 固定对战
- 每方 6 只精灵，开局客户端选择首发
- 魔力值初始 4，一方倒下 4 只判负
- 50 回合上限，第 51 回合结算：
  1. 魔力值高者胜
  2. 魔力值相同则剩余血量总和高者胜
  3. 再相同则平局
- 速度决定技能先后手；速度相同随机
- 应对机制独立在 `sim/counter.py`：技能通过 `counter_target` 标记可应对的类型（attack/defense/status）；敌方使用对应类型时，本技能应对成功并强制先手
- 全局中聚能统一视为状态技能
- buff 层数上限 99；值为 0 表示没有该 buff；lifesteal 仅允许正值
- 技能判断独立在 `sim/skill_utils.py`：可判断类型、能耗、属性是否相同、是否有 counter 效果（接口 `has_counter_effect`）
- 防御技能使用后进入 1 回合冷却；使用任意防御后，该精灵所有防御技能都进入冷却；回合结束结算时，本回合使用过防御则进入冷却，未使用则清除冷却；冷却中不允许选择防御技能，不吞回合
- 主动换人优先级最高
- 精灵倒下后的换人不占用回合，先换人再进入下一回合
- 已经死亡的精灵不能被换上场
- 主客场：开局随机决定，只影响行动后的减益/印记/特性结算顺序，不影响技能先后手
- 对方技能初始不可见，使用后只显示名称和原始能耗；己方技能显示实际能耗（含 buff/印记修正）；双方 buff/印记互相可见
- 对方速度始终显示范围
- 对方血量显示百分比
- 客户端操作：X 聚能，1-4 技能，E1-E6 换人，esc 逃跑
- 能量上限 10，能量不足不允许选择技能，不自动聚能，不占回合
- 伤害计算已接入 buff 修正：物攻/魔攻/物抗/魔抗/技能威力/连击/吸血
- 过载基础结构：overload_next / overload_current
- 萌化已实现，逻辑在 evolution.py
- 印记系统基础已实现：每方一个正面印记槽和一个负面印记槽，同种印记可叠层，上限 99；获得不同种同类印记会替换旧印记
- 脱离/离场机制已实现：技能含“自己脱离/紧急脱离/自己离场”等会让己方立即换人；“敌方脱离/敌方紧急脱离/使敌方精灵返场”会让对方立即换人；换人发生在当回合各种结算之前，并触发棘刺/降灵等离场印记
- 已实现印记效果：id 0 攻击印记（全技能威力+10%/层）；id 1 棘刺印记（主动换人/脱离下场时，新入场精灵失去6%最大生命/层）；id 2 蓄势印记（全技能威力+30%/层，所有技能能耗+1/层）；id 3 减速印记（速度-10/层）；id 4 中毒印记（回合结束3%毒系伤害/层，先于中毒效果结算；毒系精灵不免疫中毒印记）；id 5 降灵印记（主动换人/脱离下场时，新入场精灵失去1能量/层）；id 6 蓄电印记（入场后首次行动的攻击技能威力+10/层，通过独立迸发模块实现）；id 7 星陨印记（非幻系攻击触发，消耗全部层数，星陨威力 = n²+24n-24，且吃技能威力类 buff/印记）；id 8 萌芽印记（获得增益时额外获得一层，接口 `marks.amplify_buff_gain`）；id 9 龙噬印记（释放实际能耗为3的技能后，双攻+30%/层）；id 10 光合印记（回合结束回复1能量/层）；id 11 湿润印记（全技能能耗-1/层）；id 12 风气印记（先手攻击时，本次技能威力+20%/层）

## 增益/减益规则

持续时间：

- 永久：下场再上场不消失
- 普通：下场时消失
- 临时：下回合结束以后消失

类型：

- 百分比类，每层 10%：物攻、魔攻、物抗、魔抗、技能威力%、吸血、连击%
- 数值类，每层 10：速度、技能威力数值
- 整数类，每层 1：连击数、能耗、过载、先手

纯减益：

- 中毒：每层回合结束 3% 毒系伤害，毒系免疫
- 灼烧：每层回合结束 2% 火系伤害，层数减半向下取整，火系免疫
- 眩晕：每层下一回合无法行动，回合结束减 1 层
- 寄生：每层回合结束从来源吸收 2% 生命，草系免疫
- 冻结：每层 5% 生命，冰系免疫，下场不消失
- 萌化：每层退化到上一阶，可退化精灵才能获得，下场不消失
- 禁足：处于禁足状态时精灵无法离场，且无法获得新的禁足状态；持续回合由施加方传入 value
- 引电：获得 2 层时立即受到 25% 电系伤害并失去 2 层，电系免疫（代码 id 为 lightning）

属性伤害公式：

```text
伤害 = 最大生命 × 百分比 × 层数 × 属性克制倍率
```

## 待做

- 技能效果注册表（skill_effects）
- 技能实际施加 buff / 印记 / 特性
- 印记模块
- 特性模块
- 天气 / 场地
- 血脉
- 过载实际接入技能使用次数
- 萌化与进化链进一步校验


代码要求：

1. 不要肆意写输出语句，程序结果必须精简。
2. 禁止使用 try-catch 结构。
3. 不准用模拟数据仿真数据替代结果。
4. git 和 gh 只准用于读取信息，不允许进行提交等操作。

所有回复均以 "graduate asap" 结束